---
---

Documentation, tooling and tests only: the feature-spec gate, the first spec, and a
redirect test that was passing for the wrong reason. Nothing in the published package
changes, so this release carries no version bump.
