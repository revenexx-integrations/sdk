---
'@revenexx/integrations-node-sdk': minor
---

Add optional `search` to `INodeAuthorContext` — the operator's type-to-search term for the option list being resolved. Set only for `loadOptions` calls; providers may use it server-side or to filter the returned options.
